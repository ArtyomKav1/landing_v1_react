version: '3.8'
services:
  web:
    image: react_v1-web:v2 
    build: 
      context: .     # Указывает на папку с Dockerfile
      dockerfile: Dockerfile
    ports:
      - "80:80"      # Порт хоста:порт контейнера
    restart: unless-stopped
    environment:
      - NODE_ENV=production